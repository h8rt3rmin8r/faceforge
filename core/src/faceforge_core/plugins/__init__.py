"""Plugin discovery and manifest parsing (MVP).

Plugins are installed as folders under `${FACEFORGE_HOME}/plugins`.
Each plugin folder may contain a `plugin.json` manifest.

Compute runners and UI integration are out of scope for this module.
"""
